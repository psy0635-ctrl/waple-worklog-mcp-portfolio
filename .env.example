# 이 파일을 .env 로 복사한 뒤 실제 값을 채워 넣으십시오.
# .env 는 .gitignore 에 등록되어 있어 커밋되지 않습니다.

# Waple API 서버 주소 (예: https://your-waple-host)
WAPLE_BASE_URL=

# Waple API 키 (Waple → 환경설정 → API설정 에서 발급)
# stdio 모드에서는 waple_login 도구가 이 값을 자동으로 기록합니다.
# HTTP 모드에서는 저장하지 않고 요청 헤더(x-api-key)로만 전달받습니다.
WAPLE_API_KEY=
